[package]
name = "pac"
version = "0.1.0"
edition = "2021"

[features]
rt = []
critical-section = ["dep:critical-section"]

[dependencies]
vcell = "0.1"
critical-section = { version = "1", optional = true }
