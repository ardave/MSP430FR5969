[package]
name = "hal_consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
hal = { path = "../hal", features = ["rt", "critical-section"] }
msp430 = { version = "0.4.1", features = ["critical-section-single-core"] }
