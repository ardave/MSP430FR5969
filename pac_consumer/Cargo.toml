[package]
name = "pac_consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
pac = { path = "../pac", features = ["critical-section", "rt"] }
msp430 = { version = "0.4.1", features = ["critical-section-single-core"] }
